<% if activity.trackable %>
<p><%= link_to activity.owner.name, user_path(activity.owner.name) %> is now friends with <%= link_to  activity.recipient.name, user_path(activity.recipient.id) %></p>
<% end %>